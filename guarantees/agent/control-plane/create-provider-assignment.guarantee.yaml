schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.control-plane.create-provider-assignment.172
journeyIndex: 172
type: agent
subtype: control-plane
journey: Create Provider Assignment
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: "Verify API control-plane behavior for Create Provider Assignment."
status: active
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    scene:
  required: false
api:
  required: true
  verifierRefs:
    - api.agent.provider-assignment.created
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - api.agent.provider-assignment.created
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.agent.provider-assignment.created
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "API companion guarantee for Create Provider Assignment."

