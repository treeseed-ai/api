schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.control-plane.persist-usage-actual.175
journeyIndex: 175
type: agent
subtype: control-plane
journey: Persist Usage Actual
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: "Verify API control-plane behavior for Persist Usage Actual."
status: planned
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    scene:
  required: false
api:
  required: true
  verifierRefs:
    - api.agent.usage-actual.persisted
    - api.capacity.usage.repository-fails-closed
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - api.agent.usage-actual.persisted
    - api.capacity.usage.repository-fails-closed
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.agent.usage-actual.persisted
      - api.capacity.usage.repository-fails-closed
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "API companion guarantee for Persist Usage Actual."
  - "Exactly-once reservation settlement is the sole task-usage writer; the former uncalled direct writer and fail-open provider-snapshot lookup are removed."
