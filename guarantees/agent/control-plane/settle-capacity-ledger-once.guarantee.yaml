schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.control-plane.settle-capacity-ledger-once.176
journeyIndex: 176
type: agent
subtype: control-plane
journey: Settle Capacity Ledger Once
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: "Verify API control-plane behavior for Settle Capacity Ledger Once."
status: active
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    scene:
  required: false
api:
  required: true
  verifierRefs:
    - api.agent.capacity-ledger.settled-once
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - api.agent.capacity-ledger.settled-once
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.agent.capacity-ledger.settled-once
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "API companion guarantee for Settle Capacity Ledger Once."

