schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.control-plane.transition-assignment-lease-state.173
journeyIndex: 173
type: agent
subtype: control-plane
journey: Transition Assignment Lease State
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: "Verify API control-plane behavior for Transition Assignment Lease State."
status: planned
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - api.agent.assignment-lease.transitioned
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - api.agent.assignment-lease.transitioned
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.agent.assignment-lease.transitioned
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "API companion guarantee for Transition Assignment Lease State."

