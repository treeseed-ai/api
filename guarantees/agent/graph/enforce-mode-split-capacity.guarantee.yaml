schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.graph.enforce-mode-split-capacity.314
journeyIndex: 314
type: agent
subtype: graph
journey: Enforce Mode Split Capacity
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: "Planning and acting reservations respect workday mode split min, max, and hard-cap policy."
status: active
actors:
  allowed: [project_manager, capacity_operator, team_owner]
  forbidden: [anonymous_user, unauthorized_user]
gates: [core, release]
preconditions:
  fixtures: [integrated_treeseed_project]
api:
  required: true
  verifierRefs: [api.agent.graph.enforce-mode-split-capacity]
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs: [api.agent.graph.enforce-mode-split-capacity]
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs: [api.agent.graph.enforce-mode-split-capacity]
evidence:
  required: [guarantee_report, verifier_evidence]
