schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.graph.reserve-planning-capacity.313
journeyIndex: 313
type: agent
subtype: graph
journey: Reserve Planning Capacity
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: "Synthesized planning assignments create planning capacity reservations before lease."
status: planned
actors:
  allowed: [project_manager, capacity_operator, team_owner]
  forbidden: [anonymous_user, unauthorized_user]
gates: [core, release]
preconditions:
  fixtures: [integrated_treeseed_project]
api:
  required: true
  verifierRefs: [api.agent.graph.reserve-planning-capacity]
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs: [api.agent.graph.reserve-planning-capacity]
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs: [api.agent.graph.reserve-planning-capacity]
evidence:
  required: [guarantee_report, verifier_evidence]
notes:
  - "Planned until reservation and assignment creation are atomic and every billable planning assignment carries a durable reservation."
