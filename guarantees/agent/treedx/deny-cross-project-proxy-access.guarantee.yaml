schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.treedx.deny-cross-project-proxy-access.178
journeyIndex: 178
type: agent
subtype: treedx
journey: Deny Cross Project Proxy Access
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: "Verify API control-plane behavior for Deny Cross Project Proxy Access."
status: planned
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    scene:
  required: false
api:
  required: true
  verifierRefs:
    - api.agent.treedx.cross-project-denied
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - api.agent.treedx.cross-project-denied
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.agent.treedx.cross-project-denied
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "API companion guarantee for Deny Cross Project Proxy Access."
