schemaVersion: treeseed.guarantee/v1
id: guarantee.api.endpoints.agent-graphs-and-workdays.407
journeyIndex: 407
type: api
subtype: endpoints
journey: Agent Graph And Workday Endpoint Reliability
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: "Decision graph, deliverable, workday, assignment, and agent runtime endpoints are descriptor-covered and graph workflow-verified."
status: active
actors:
  allowed: [project_manager, capacity_operator, team_owner]
  forbidden: [anonymous_user, unauthorized_user]
gates: [core, release]
preconditions:
  fixtures: [integrated_treeseed_project]
api:
  required: true
  verifierRefs: [api.endpoints.agent-graphs-and-workdays]
audit:
  required: true
  verifierRefs: [api.endpoints.agent-graphs-and-workdays]
negativeCases:
  - id: acting-blocked-until-graph-ready
    verifierRefs: [api.endpoints.agent-graphs-and-workdays]
evidence:
  required: [guarantee_report, api_acceptance_report]

