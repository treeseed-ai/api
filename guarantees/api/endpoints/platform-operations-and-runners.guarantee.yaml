schemaVersion: treeseed.guarantee/v1
id: guarantee.api.endpoints.platform-operations-and-runners.405
journeyIndex: 405
type: api
subtype: endpoints
journey: Platform Operation Endpoint Reliability
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: "Platform operation, job, and runner endpoints expose only the expected platform-admin and runner trust boundaries."
status: active
actors:
  allowed: [platform_admin, platform_runner]
  forbidden: [anonymous_user, team_viewer]
gates: [core, release, security]
preconditions:
  fixtures: [integrated_treeseed_project]
api:
  required: true
  verifierRefs: [api.endpoints.platform-operations-and-runners]
audit:
  required: true
  verifierRefs: [api.endpoints.platform-operations-and-runners]
negativeCases:
  - id: team-viewer-platform-operation-denied
    verifierRefs: [api.endpoints.platform-operations-and-runners]
evidence:
  required: [guarantee_report, api_acceptance_report]

