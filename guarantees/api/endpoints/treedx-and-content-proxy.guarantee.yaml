schemaVersion: treeseed.guarantee/v1
id: guarantee.api.endpoints.treedx-and-content-proxy.408
journeyIndex: 408
type: api
subtype: endpoints
journey: TreeDX And Content Proxy Endpoint Reliability
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: "TreeDX routing, content proxy, library, and local content endpoints are descriptor-covered with project-scoped authorization."
status: active
actors:
  allowed: [project_member, team_owner, provider_key]
  forbidden: [anonymous_user, non_member]
gates: [core, release, security]
preconditions:
  fixtures: [integrated_treeseed_project]
api:
  required: true
  verifierRefs: [api.endpoints.treedx-and-content-proxy]
audit:
  required: true
  verifierRefs: [api.endpoints.treedx-and-content-proxy]
negativeCases:
  - id: cross-project-content-access-denied
    verifierRefs: [api.endpoints.treedx-and-content-proxy]
evidence:
  required: [guarantee_report, api_acceptance_report]

