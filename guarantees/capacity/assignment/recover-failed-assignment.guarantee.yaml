schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity.assignment.recover-failed-assignment.084
journeyIndex: 84
type: capacity
subtype: assignment
journey: Recover Failed Assignment
ownerPackage: "@treeseed/api"
summary: Return expire or retry a failed assignment with traceable reason while preserving API lease and settlement integrity.
status: active
dependencies:
  journeys:
    - 82
    - 83
  guarantees:
    - guarantee.capacity.assignment.inspect-assignment-leases.082
    - guarantee.capacity.usage.review-capacity-usage.083
actors:
  allowed:
    - capacity_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 82, 83"
scene:
  required: true
  manifest: ./scenes/recover-failed-assignment.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/capacity/runtime
  componentContract: record-modal-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.capacity.assignment.recover-failed-assignment.api
content:
  required: false
  verifierRefs:
    - api.capacity.assignment.recover-failed-assignment.content
audit:
  required: true
  verifierRefs:
    - api.capacity.assignment.recover-failed-assignment.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.capacity.assignment.recover-failed-assignment.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Return expire or retry a failed assignment with traceable reason while preserving API lease and settlement integrity.
