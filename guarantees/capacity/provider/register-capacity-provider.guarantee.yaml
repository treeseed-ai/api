schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity.provider.register-capacity-provider.080
journeyIndex: 80
type: capacity
subtype: provider
journey: Register Capacity Provider
ownerPackage: "@treeseed/api"
summary: Create provider identity capabilities constraints and team eligibility before key rotation approval or deployment.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - capacity_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/register-capacity-provider.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/capacity/providers/new
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.capacity.provider.register-capacity-provider.api
content:
  required: false
  verifierRefs:
    - api.capacity.provider.register-capacity-provider.content
audit:
  required: true
  verifierRefs:
    - api.capacity.provider.register-capacity-provider.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.capacity.provider.register-capacity-provider.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create provider identity capabilities constraints and team eligibility before key rotation approval or deployment.
