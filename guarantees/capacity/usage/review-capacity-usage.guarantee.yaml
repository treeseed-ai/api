schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity.usage.review-capacity-usage.083
journeyIndex: 83
type: capacity
subtype: usage
journey: Review Capacity Usage
ownerPackage: "@treeseed/api"
summary: Show usage actuals token counts durations budgets and ledger settlement records for providers projects agents and workdays.
status: planned
dependencies:
  journeys: []
  guarantees: []
actors:
  allowed:
    - capacity_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 29, 63"
scene:
  required: true
  manifest: ./scenes/review-capacity-usage.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/capacity
  componentContract: dashboard/read-model service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.capacity.usage.review-capacity-usage.api
content:
  required: false
  verifierRefs:
    - api.capacity.usage.review-capacity-usage.content
audit:
  required: true
  verifierRefs:
    - api.capacity.usage.review-capacity-usage.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.capacity.usage.review-capacity-usage.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Show usage actuals token counts durations budgets and ledger settlement records for providers projects agents and workdays.
