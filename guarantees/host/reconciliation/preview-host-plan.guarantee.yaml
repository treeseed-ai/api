schemaVersion: treeseed.guarantee/v1
id: guarantee.host.reconciliation.preview-host-plan.074
journeyIndex: 74
type: host
subtype: reconciliation
journey: Preview Host Plan
ownerPackage: "@treeseed/api"
summary: Compile desired hosting state and show create update delete adopt retain actions before any provider mutation; required because hosting changes must route through trsd reconciliation.
status: planned
dependencies:
  journeys:
    - 23
  guarantees:
    - guarantee.host.host.create-host.023
actors:
  allowed:
    - host_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 23"
scene:
  required: true
  manifest: ./scenes/preview-host-plan.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/hosts/preview-host-plan
  componentContract: dashboard/read-model guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.host.reconciliation.preview-host-plan.api
content:
  required: false
  verifierRefs:
    - todo.host.reconciliation.preview-host-plan.content
audit:
  required: true
  verifierRefs:
    - todo.host.reconciliation.preview-host-plan.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.host.reconciliation.preview-host-plan.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Compile desired hosting state and show create update delete adopt retain actions before any provider mutation; required because hosting changes must route through trsd reconciliation.
