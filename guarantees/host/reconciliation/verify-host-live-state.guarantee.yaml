schemaVersion: treeseed.guarantee/v1
id: guarantee.host.reconciliation.verify-host-live-state.075
journeyIndex: 75
type: host
subtype: reconciliation
journey: Verify Host Live State
ownerPackage: "@treeseed/api"
summary: Refresh provider state and verify postconditions after reconciliation; ok true is valid only after live observation passes.
status: active
dependencies:
  journeys:
    - 23
    - 24
  guarantees:
    - guarantee.host.host.create-host.023
    - guarantee.host.host.edit-host.024
actors:
  allowed:
    - host_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 23, 24"
scene:
  required: true
  manifest: ./scenes/verify-host-live-state.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/hosts
  componentContract: dashboard/read-model service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.host.reconciliation.verify-host-live-state.api
content:
  required: false
  verifierRefs:
    - api.host.reconciliation.verify-host-live-state.content
audit:
  required: true
  verifierRefs:
    - api.host.reconciliation.verify-host-live-state.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.host.reconciliation.verify-host-live-state.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Refresh provider state and verify postconditions after reconciliation; ok true is valid only after live observation passes.
