schemaVersion: treeseed.guarantee/v1
id: guarantee.project.treedx.configure-treedx-routing.088
journeyIndex: 88
type: project
subtype: treedx
journey: Configure TreeDX Routing
ownerPackage: "@treeseed/api"
summary: Configure project-scoped TreeDX proxy handles and authorization; agents and operations must use TreeDX rather than direct local content access.
status: planned
dependencies:
  journeys: []
  guarantees: []
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 32"
scene:
  required: true
  manifest: ./scenes/configure-treedx-routing.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/hosts/knowledge-library
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.project.treedx.configure-treedx-routing.api
content:
  required: false
  verifierRefs:
    - api.project.treedx.configure-treedx-routing.content
audit:
  required: true
  verifierRefs:
    - api.project.treedx.configure-treedx-routing.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.project.treedx.configure-treedx-routing.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Configure project-scoped TreeDX proxy handles and authorization; agents and operations must use TreeDX rather than direct local content access.
