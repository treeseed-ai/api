schemaVersion: treeseed.guarantee/v1
id: guarantee.project.treedx.verify-treedx-access.089
journeyIndex: 89
type: project
subtype: treedx
journey: Verify TreeDX Access
ownerPackage: "@treeseed/api"
summary: Run a read/write authorization check against the project TreeDX route before workdays or content operations depend on it.
status: active
dependencies:
  journeys:
    - 88
  guarantees:
    - guarantee.project.treedx.configure-treedx-routing.088
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 88"
scene:
  required: true
  manifest: ./scenes/verify-treedx-access.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/treedx/verify-treedx-access
  componentContract: record-action guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.project.treedx.verify-treedx-access.api
content:
  required: false
  verifierRefs:
    - api.project.treedx.verify-treedx-access.content
audit:
  required: true
  verifierRefs:
    - api.project.treedx.verify-treedx-access.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - api.project.treedx.verify-treedx-access.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Run a read/write authorization check against the project TreeDX route before workdays or content operations depend on it.
