schemaVersion: treeseed.guarantee/v1
id: guarantee.security.credential-custody.workflow-secret-custody.734
journeyIndex: 734
type: security
subtype: credential-custody
journey: Configure Workflow Secrets Without Plaintext Custody
ownerPackage: "@treeseed/api"
surface: api-control-plane
summary: The browser encrypts secret values to GitHub, the runner consumes ciphertext once, and persistent evidence contains only metadata and digests.
status: blocked
actors: { allowed: [credential_administrator], forbidden: [unauthorized_user, capacity_provider] }
devices: { required: [desktop_chromium, mobile_chromium] }
gates: [security, release]
preconditions: { fixtures: [integrated_treeseed_project], notes: [A Workflow Connector secret enclave is required.] }
api: { required: true, verifierRefs: [] }
audit: { required: true, verifierRefs: [] }
negativeCases: []
evidence: { required: [api_acceptance_report, secret_scan_report, audit_log, cleanup_report] }
notes: [Blocked until live secret and variable create-update-delete, replay, redaction, and cleanup acceptance passes twice.]
