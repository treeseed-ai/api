schemaVersion: treeseed.guarantee/v1
id: guarantee.workday.diagnostics.inspect-mode-run-trace.087
journeyIndex: 87
type: workday
subtype: diagnostics
journey: Inspect Mode Run Trace
ownerPackage: "@treeseed/api"
summary: Show agent mode class handler assignment provider runner execution provider inputs outputs token counts durations errors and diagnostics for forensic traceability.
status: planned
dependencies:
  journeys:
    - 63
  guarantees:
    - guarantee.workday.workday.monitor-workdays.063
actors:
  allowed:
    - project_manager
    - agent_operator
    - capacity_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 63"
scene:
  required: true
  manifest: ./scenes/inspect-mode-run-trace.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/workdays/inspect-mode-run-trace
  componentContract: dashboard/read-model guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.workday.diagnostics.inspect-mode-run-trace.api
content:
  required: false
  verifierRefs:
    - todo.workday.diagnostics.inspect-mode-run-trace.content
audit:
  required: true
  verifierRefs:
    - todo.workday.diagnostics.inspect-mode-run-trace.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.workday.diagnostics.inspect-mode-run-trace.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Show agent mode class handler assignment provider runner execution provider inputs outputs token counts durations errors and diagnostics for forensic traceability.
